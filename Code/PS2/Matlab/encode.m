function [code] = encode(ntuple,nfirms,binom)
% This procedure takes a weakly descending n-tuple (n = nfirms) and encodes 
% it into an integer state code
%%%%%%%% Task ********

%%%%%%%%%%%%%%%%%%%%%
end