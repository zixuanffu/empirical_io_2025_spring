function [ntuple] = decode(code,nfirms,binom)
% This procedure takes a previously encoded state code, and decodes it into
% a weakly descending n-tuple (n = nfirms, number of active firms)

%%% Task %%%%%

%%%%%%%%%%%%%%
end